// Create a variable to store the export.modules objects
const pattern = require("./mathUtil");

console.log(pattern.counter(["shaun", "crystal", "ryu"]));
console.log(pattern.adder(5, 10));
console.log(pattern.adder(pattern.pi, 5));
